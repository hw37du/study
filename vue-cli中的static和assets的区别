首先，从词意上来看asset和static
asset是指有用的资源，资产，等等
static其实也是指上述的哪些类型，只不过它是静态的，所谓的静态是指不参与webpack的打包
 
 其次，从它们的,优点来讲
 asset
 1，只是资源不变，就可以通过浏览器的缓存机制来缓存资源
 2，webpack可以将有写image转换成base64格式的，从而避免了网络请求,提高了相应速度
 
 static
 1，只要资源不变，就可以通过浏览器的缓存机制来缓存资源
 2，可以通过完整的url定位到资源
 
 其次，需要了解webpack如何对待static和asset文件的
 1，
